# Tasks — 001-responsive-layout

| # | Task | Owner | Status |
|---|---|---|---|
| 1 | Define `@theme` block in `src/styles.css` with all BLISS color tokens, font families (`Inter`, `Outfit`), radii (`rounded-3xl`), and keyframe animations (`float`, `fade-in`, `slide-up`, `banner-in`, `banner-out`) | Frontend-Design-Agent | Pending |
| 2 | Install `@fortawesome/fontawesome-free` and import CSS in `styles.css` | Frontend-Design-Agent | Pending |
| 3 | Create `shared/services/mock-navigation.service.ts` with `activeTabIndex: Signal<number>` and `navigate(tabIndex)` method | TypeScript-Expert-Agent | Pending |
| 4 | Create `shell/shell.component.ts` as Standalone component with centered viewport mockup logic, responsive breakpoint detection via CSS | Frontend-Design-Agent | Pending |
| 5 | Create `shell/nav-bar/nav-bar.component.ts` with 4 FontAwesome icons, Signal-bound active state, router navigation on tap | Frontend-Design-Agent | Pending |
| 6 | Create `shell/header/header.component.ts` with "BLISS" wordmark in `font-bliss-display` and notification bell icon | Frontend-Design-Agent | Pending |
| 7 | Update `app.routes.ts` with route definitions for `/dashboard`, `/mood`, `/community`, `/profile` (all pointing to placeholder components initially) | TypeScript-Expert-Agent | Pending |
| 8 | Verify build: run `ng build` and confirm no errors | Bliss-Orchestrator | Pending |
